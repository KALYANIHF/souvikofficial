{
  "version": 2,
  "builds": [
    { "src": "serverside/server.js", "use": "@vercel/node" },
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [{ "src": "/api/(.*)", "destination": "/api/$1" }],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
